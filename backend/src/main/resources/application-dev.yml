spring:
  datasource:
    url: jdbc:mysql://localhost:3306/royal_stay_dev?createDatabaseIfNotExist=true
    username: root
    password: password
  
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

logging:
  level:
    root: INFO
    com.royalstay: DEBUG
